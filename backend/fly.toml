app = "suswastha-fastapi"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "10000"

[http_service]
  internal_port = 10000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]
